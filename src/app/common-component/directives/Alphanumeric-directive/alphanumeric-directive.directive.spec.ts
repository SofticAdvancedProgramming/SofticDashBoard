import { AlphanumericDirectiveDirective } from './alphanumeric-directive.directive';

describe('AlphanumericDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphanumericDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
