import { NoEmailFormatDirective } from './no-email-format.directive';

describe('NoEmailFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new NoEmailFormatDirective();
    expect(directive).toBeTruthy();
  });
});
